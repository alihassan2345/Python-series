#  if statement is used to check a condition: if the condition is true, 
#  we run a block of statements (called the if-block),
#  else we process another block of statements (called the else-block).

person = int(input("Enter your age: "))
if person > 18:
    print("You are eligible to vote")
